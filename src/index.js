// This function says hi to someone when called with their name as an argument
function sayHi(name) {
  return `Hello ${name}`
}

//This function says bye to someone when called with their name as an argument
function sayBye(name) {
  return `Bye ${name}`
}

module.exports = sayHi
